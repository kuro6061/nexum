version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nexum
      POSTGRES_USER: nexum
      POSTGRES_PASSWORD: nexum
    ports:
      - "5432:5432"
    volumes:
      - nexum_pgdata:/var/lib/postgresql/data

volumes:
  nexum_pgdata:
